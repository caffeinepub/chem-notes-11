// This file is intentionally minimal as the backend is empty
// and all data is served from static frontend assets

export function useQueries() {
  // No backend queries needed for this static notes website
  return {};
}
